import { UserTypeRule.Model } from './user-type-rule.model';

describe('UserTypeRule.Model', () => {
  it('should create an instance', () => {
    expect(new UserTypeRule.Model()).toBeTruthy();
  });
});
