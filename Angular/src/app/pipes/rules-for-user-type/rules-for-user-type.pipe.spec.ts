import { RulesForUserTypePipe } from './rules-for-user-type.pipe';

describe('RulesForUserTypePipe', () => {
  it('create an instance', () => {
    const pipe = new RulesForUserTypePipe();
    expect(pipe).toBeTruthy();
  });
});
