import { UserTypeForRulePipe } from './user-type-for-rule.pipe';

describe('UserTypeForRulePipe', () => {
  it('create an instance', () => {
    const pipe = new UserTypeForRulePipe();
    expect(pipe).toBeTruthy();
  });
});
